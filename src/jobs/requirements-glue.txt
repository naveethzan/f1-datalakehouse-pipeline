# AWS Glue job specific requirements
# These packages will be installed in the Glue job environment

# Data validation
great-expectations==0.18.8

# Data lineage tracking
openlineage-python==1.8.0

# Additional utilities that might be needed
pyyaml>=6.0
boto3>=1.26.0
botocore>=1.29.0
urllib3<2.0  # Fix for DEFAULT_CIPHERS import error with great-expectations

# Note: Spark, PySpark, and AWS Glue libraries are pre-installed in Glue environment
# Note: pandas, numpy are also pre-installed in Glue 4.0